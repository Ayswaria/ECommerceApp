export interface Product {
  id: number;
  name: string;
  description: string;
  price: number;
  strikePrice: number;
  category: string;
  image: string;
  rating: number;
  stock: number;
  tag: string;
}