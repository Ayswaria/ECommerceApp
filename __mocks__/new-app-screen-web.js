// Stub for @react-native/new-app-screen — not usable on web
const React = require('react');
const { View } = require('react-native');

const NewAppScreen = () => React.createElement(View, null);

module.exports = { NewAppScreen };
module.exports.default = NewAppScreen;
